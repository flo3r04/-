#include <stdio.h>
int main()
{
	int year;
	
	printf("연도 입력 : ");
	scanf_s("%d", &year);

	if ((year % 4 == 0 && year % 100 != 0)|| year % 400 == 0) 
	{
		printf("%d = 윤년", year);
	}
	else

		printf("%d = 윤년이 아님", year);
	
	
	
	return 0;
}
